# Warlock Studio audit — 2026-09-06

**Recommendation:** all four findings were built on 2026-09-06 and are struck below. What is
left owed is coverage, not defects: re-run this audit over the parts of the row named under
Coverage, which this pass did not read.

**4 findings: 0 Critical, 1 High, 2 Medium, 1 Low; 0 human-only. All four closed 2026-09-06.**

Audited: version **0.0.37**, working tree at **`daa197b8`**. Dirty at start:

```
 M TODO.md
 M docs/manual/29-inker-animation.md
 M src/warlock/studio/inker_keys.py
 M src/warlock/studio/inker_ops.py
 M src/warlock/studio/inker_state.py
 M src/warlock/studio/manual/targets.py
 M src/warlock/studio/panes/inker_canvas.py
 M src/warlock/studio/panes/inker_preview.py
 M src/warlock/studio/panes/inker_textures.py
 M src/warlock/studio/skeletons.py
 M tests/inker/test_inker_ux.py
 M tests/test_layout.py
 M tests/test_layouts.py
 M uv.lock
?? src/warlock/studio/inker/walk/
?? src/warlock/studio/inker_walk.py
?? src/warlock/studio/panes/inker_walk.py
?? src/warlock/studio/panes/inker_walk_canvas.py
?? tests/inker/test_walk_session.py
?? tests/inker/walk/
```

None of the dirty paths is in this slice; they are in-progress Inker walk-cycle work and
were not read as part of the audit. Scope: `create` → slice `create`.
`uv run ruff check .`: All checks passed. The suite was not run by this audit; findings are
from reading. No probe was written: every finding below is *Inspected*, so the probes
directory is empty.

**Vocabulary.** *Critical*: crash, data loss, or corrupts a document. *High*: a wrong
result the user will hit, or a promise in the docs the code breaks. *Medium*: a defect
with a workaround, or a hard-invariant violation not yet reachable. *Low*: hardening,
consistency, naming. *Reproduced*: an executable example confirmed it. *Inspected*:
follows from the current code path. *Evidence gap*: release proof is missing from the
repository, not a demonstrated failure.

**Second-look.** The one High below was re-read by the orchestrator against the working
tree: `texture_panel.py` was confirmed to contain no `forms.Form`, no `errors=`, no
`clear_field_errors`, while `remesh_panel.py` and `retarget_panel.py` each carry all
three, and `_jobs_rework.retexture_job` was confirmed to raise with `field="strength"`,
`"control"`, `"control_scale"` and `"texture_size"` against controls that pane draws.
Downgraded during merge: **create-03** (reported as High, "an upgraded user's Create form
can open as the wrong asset type"), because `create_assets.selected()` already prefers
`asset_type` over `generation_type` and `sync_legacy_fields` rewrites both on the first
Create draw, so no wrong door survives to a submit; what remains is a migration that
contradicts its own comment, which is Medium.

## Critical

None.

## High

| ID / where | Finding and evidence | Recommended fix |
|---|---|---|
| **create-01 — Create: re-texture panel** | ~~**A re-texture refusal rings no control.**~~ [panes/texture_panel.py:39-211](../src/warlock/studio/panes/texture_panel.py) draws `prompt`, `strength` and `control_scale`, and [service/_jobs_rework.py:216-254](../src/warlock/service/_jobs_rework.py) refuses each of them by `field` name — but the pane never wraps its controls in `forms.Form(errors=ctx.state.field_errors, on_edit=ctx.state.clear_field_error)` and never calls `ctx.state.clear_field_errors()` on submit, unlike both sibling rework panes. *Inspected.* The refusal reaches the user as a bare toast with nothing marked, and a previous refusal's rings are never cleared; this is the defect `tests/test_field_error_wiring.py` was written for after it was found in `retarget_panel.py`, and that file's `FIELD_FORMS`, `ELSEWHERE` and `cases` tables do not name `texture_panel.py`, so its sweep cannot see the gap. Most reachable via a reroll that re-submits a stored `strength`/`control_scale` predating a tightened `models.RETEXTURE_STRENGTH_*`/`CONTROL_SCALE_*` range, which [_jobs_resubmit.py:339-361](../src/warlock/service/_jobs_resubmit.py) does not re-check. **Built 2026-09-06:** the pane's controls now open `forms.Form("retexture-settings", errors=..., on_edit=...)`, `_submit` clears the rings on each press, and `texture_panel.py` was added to `FIELD_FORMS` so the sweep covers it from now on. Test: `test_a_retexture_refusal_rings_the_control_on_texture_panel`, which failed before the fix with `texture_panel never opens forms.Form('retexture-settings')`. | Wrap the pane's controls in `forms.Form("retexture-settings", errors=ctx.state.field_errors, on_edit=ctx.state.clear_field_error)`, clear the rings at the top of `_submit`'s pressed branch, and add the pane to `FIELD_FORMS` so the sweep covers it from now on. Regression: `test_a_retexture_refusal_rings_the_control_on_texture_panel`. |

## Medium

| ID / where | Finding and evidence | Recommended fix |
|---|---|---|
| **create-02 — Create: the three rework panels** | ~~**A collision with an in-flight rework is discovered by pressing the button.**~~ [panes/retarget_panel.py:186-217](../src/warlock/studio/panes/retarget_panel.py), [remesh_panel.py:158-176](../src/warlock/studio/panes/remesh_panel.py) and [texture_panel.py:185-201](../src/warlock/studio/panes/texture_panel.py) grey their submit only on their own client-side busy flag, never on `_jobs_lifecycle.dependent_jobs`, so with a sibling rework already queued against the same asset the button stays live and the `Conflict` arrives after the press. *Inspected.* Manual chapter 23 frames these refusals as things the panel will not hide, and shows the stale-rig warning *before* the button; this one is only visible after a wasted submit. **Built 2026-09-06:** each of the three panels grew a pure `dependent_job_reason()` beside its draw call, read from the frame's `jobs_cache` window rather than the store, and greys the button with that reason. Tests: `tests/test_rework_panel_conflicts.py`, five cases; `test_retarget_panel_greys_rebuild_button_while_a_dependent_retexture_is_running` failed before the fix with `no attribute 'dependent_job_reason'`. Manual chapter 23 gained the sentence. | Read the dependent-job count before drawing the button and disable it with a reason naming the job in flight, in the place the stale-rig warning already occupies. Regression: `test_retarget_panel_greys_rebuild_button_while_a_dependent_retexture_is_running`. |
| **create-03 — Create: settings migration** | ~~**The `asset_type` migration re-derives `generation_type` from the stale field it is replacing.**~~ [studio/settings.py:79-94](../src/warlock/studio/settings.py) sets `form["asset_type"]` from the legacy switches, then sets `form["generation_type"] = create_assets.asset_type_from_params(form)` — and [create_assets.py:166-172](../src/warlock/studio/create_assets.py) checks `generation_type` *first*, returning the pre-migration value in preference to the field just migrated. *Inspected.* Sharpest in the corrupt-value branch, whose own comment says a bad `asset_type` "must not resurrect a contradictory legacy selection" and which then writes back exactly that contradiction. The divergence is repaired by `sync_legacy_fields` on the first Create draw, so no wrong door is reachable today; it is a stored state that contradicts the invariant that the two fields are one choice. **Built 2026-09-06:** `_migrate` now assigns `form["generation_type"] = form["asset_type"]` directly. Test: `test_migrate_makes_the_two_asset_type_fields_agree_on_a_pre_asset_type_file`, which failed before the fix with `assert '3d_model' == 'seamless_material'`. | Set `form["generation_type"] = form["asset_type"]` directly, as `state.restore_form` already does, instead of routing through the reader that prefers the stale field. Regression: `test_migrate_makes_the_two_asset_type_fields_agree_on_a_pre_asset_type_file`. |

## Low

| ID / where | Finding and evidence | Recommended fix |
|---|---|---|
| **create-04 — Create: matte preview** | ~~**A failed task is matched to the open preview by suffix, not identity.**~~ [studio/matte_preview.py:186](../src/warlock/studio/matte_preview.py) tests `str(done.key).endswith(state.job_id)` rather than `done.key == matte_preview.key(state.job_id)`; job ids are `uuid4().hex[:12]`, so a key ending in another job's id would latch that job's failure onto the reference on screen. *Inspected.* A wrongly-latched `failed_stamp` stops `pump` re-submitting, leaving the cutout modal up with no toast and no retry until the file's mtime moves. `tests/test_matte_handoff.py::test_a_failure_for_another_reference_does_not_latch_this_one` covers a non-matching key, not a suffix-colliding one. **Built 2026-09-06:** the comparison is now `getattr(done, "key", None) != key(state.job_id)`; the fixer confirmed by grep that `pump` is the only submit site and prefixes nothing, so exact equality breaks no handoff. Test: `test_a_failure_key_that_merely_ends_with_the_open_job_id_does_not_latch_it`, which failed before the fix with `assert 1788701115489245600 is None`. | Compare the full key for equality. Regression: `test_a_failure_key_that_merely_ends_with_the_open_job_id_does_not_latch_it`. |

## Human-only — owed, not open

None. Nothing in this slice needed a card, a clean machine, a third-party editor or ears
to settle.

| ID | What has to happen | Who / what it needs |
|---|---|---|
| — | — | — |

## Coverage

- Slices run: `create`. Slices skipped: none in scope.
- Explorers: three ran over the row with split emphases (threads/undo/stale-completion/
  cancel; writes/ceilings/sweeps/presence/subprocess/recovery; greyed reasons/docs-vs-code/
  layering/blind spots/inventories). None failed or timed out, but all three stopped short
  of the row's largest files. **Not read in this pass, and therefore not audited:**
  `panes/settings_2d.py` (only `generate`/`submit_job`/`validate` were read),
  `panes/settings_3d.py`, `panes/settings_character.py`, `panes/sheet_panel.py`,
  `panes/sprite_panel.py`, `panes/stage_rig.py` and `panes/pose_panel.py` bodies; all of
  `studio/viewer/` except `camera.py` and the ceiling section of `gltf.py`;
  `studio/viewer_embed.py` and every `_view_*.py`; and the bodies of `generation.py`,
  `guidance.py`, `judge.py`, `sweep.py` and `provenance.py` (import-checked for layering
  only, which was clean). That is most of the row by line count; the four findings should
  be read as a floor, not a total.
- Checklist items not evaluable in this scope: item 1 (undo) — Create has no undo surface
  of its own; item 6 (per-frame recompute) and item 17 (cancel) were spot-checked in the
  files that were opened and not walked across the unread panes above; items 9–12, 14, 15
  and 18 were walked only over the headless modules, not the pane set.
- Verified as already correct, so not reported: the base-model registry is eleven entries,
  matching manual chapter 12; the prompt-history cap is 20 in both code and manual; the
  five top-level Create modules import no imgui, moderngl or pygame.
- Not done by this audit: no interactive pass, no fresh install, no GPU inference, no
  third-party editor validation, no dependency-advisory scan. A passing suite does not
  close those.

## Fix phase — 2026-09-06

All four findings were closed the day they were filed, by three file-disjoint fixers. Each
wrote its regression first and proved it failed against the unfixed code; the failing
assertions are quoted in the rows above. The gate was then run once, with nothing else
editing: `uv run ruff check .` clean, `uv run pytest` **18427 passed, 45 skipped** in 140 s,
`uv run python scripts/preflight.py --fast` passed. One ruff E501 surfaced on the first gate
run — the create-03 regression's name was 110 characters — and the name was shortened;
nothing else in the gate needed a second round. Nothing moved to `TODO.md`: this audit found
no human-only work.

## How this file is closed

A finding is struck the day it is built: `~~claim~~ Built YYYY-MM-DD: what was done, and
the test.` A human-only finding is moved to `TODO.md`. When nothing is left, this file is
deleted rather than ticked; `git log --diff-filter=D -- docs/audit-*.md` finds it.
Nothing under `src/` or `scripts/` may cite this filename.

---

# Warlock Studio audit — 2026-09-06, second run

**Recommendation:** all nine findings were built on 2026-09-06 and are struck below. What
remains owed on this slice is coverage, not defects — see Coverage for the ~1,220 lines
that are still unaudited.

**9 findings: 1 Critical, 2 High, 4 Medium, 2 Low; 0 human-only. All nine closed 2026-09-06.**

Audited: version **0.0.37**, working tree at **`82506b47`** (the first run's fixes are
committed; the dirty paths are unchanged in-progress Inker walk-cycle work, none of it in
this slice). Scope: the files the first run left unread — roughly 17,000 lines across six
explorers. `uv run ruff check .`: All checks passed. The suite was not run by this pass.

**Second-look.** The Critical and both Highs were re-verified by the orchestrator against
the working tree; the two with probes were re-run here, not taken on report. Downgraded
during merge: **create2-07**, filed as High on the grounds that a door turns a refusal into
an unhandled crash — the `TypeError` reproduces exactly as claimed, but
`service.jobs.create_generation_request` has no caller anywhere in `src/`, so no user path
reaches it; a hard-invariant violation not yet reachable is Medium by this file's own
vocabulary.

## Critical

| ID / where | Finding and evidence | Recommended fix |
|---|---|---|
| **create2-01 — Create: glTF loader** | ~~**A skin's own joint indices are never bounds-checked.**~~ **Built 2026-09-06:** `_Reader.skin` now raises `ValueError` naming the out-of-range index before any `Skin` is returned, and also refuses a joint count that disagrees with `inverseBindMatrices` — the same crash one line over, since `joint_palette` zips them by position. Test: `test_a_skin_joint_index_past_the_node_count_is_refused_at_load`, which failed before the fix with `DID NOT RAISE ValueError`. Original finding: [viewer/gltf.py:747](../src/warlock/studio/viewer/gltf.py) builds `Skin(joints=list(skin["joints"]))` with no check against the file's node count; only `node.mesh`/`node.skin` are checked. *Reproduced* (`probes/create2-d-01.py`, re-run by the orchestrator): `load()` succeeds on a GLB whose skin names a joint past `len(nodes)`, then [gltf.py:232](../src/warlock/studio/viewer/gltf.py) raises a bare `IndexError` out of `joint_palette` — called from `GpuModel.__init__` *after* buffers and textures for every earlier valid node are allocated. This is the same shape as the already-closed `node.skin` finding, one layer deeper: `tests/test_gltf_loader.py::test_a_glb_with_an_out_of_range_skin_index_does_not_leak_gpu_buffers_from_earlier_valid_nodes` builds a file where "Node 1 claims skin index 7, but no skins exist" and cannot see a bad index *inside* `skins[i].joints`. A glTF is untrusted input — TRELLIS output, a user's disk, a downloaded pack — and the app sets no moderngl `gc_mode`, so the buffers leak for the life of the process. | Validate every entry of `skin["joints"]` against the declared node count at read time and raise `ValueError` naming the index, the way `node()` already does, before any GPU object is built. Regression: `test_a_skin_joint_index_past_the_node_count_is_refused_at_load`. |

## High

| ID / where | Finding and evidence | Recommended fix |
|---|---|---|
| **create2-02 — Create: sheet delete** | ~~**Deleting a sheet while its restyle is running resurrects the restyle.**~~ **Built 2026-09-06:** `delete_sheet` now runs under `svc.convert_lock(job_id, "sheets")` and raises `Conflict(field="sheet_id")` while a `pixel_sheet` row targeting the sheet is active. Refuse rather than cancel-then-delete was the deliberate call. Test: `test_deleting_a_sheet_while_its_restyle_is_running_does_not_resurrect_it`, which failed before the fix with `DID NOT RAISE Conflict`; expressed as store state, not a thread race. Original finding: [service/sheets.py:231](../src/warlock/service/sheets.py) takes no lock, while `create_sheet` at [sheets.py:203](../src/warlock/service/sheets.py) runs under `svc.convert_lock(job_id, "sheets")`; [rigging.py:1302](../src/warlock/rigging.py) unlinks all four files immediately, and an in-flight `pixel_sheet` job then `os.replace`s its output back onto `sheet_pixel_path`/`sheet_pixel_png_path` from [_q_sprite.py:288,344](../src/warlock/_q_sprite.py). *Inspected* (the missing lock and the writing job were both confirmed by the orchestrator). The delete is confirmed as permanent and the manual says a sheet's restyle goes with it; what is left is an orphaned pair that `list_sheets` cannot show, because it keys off the base sidecar that was deleted — so the files sit on disk with no route back. `tests/test_pixel_sheet_service.py::test_deleting_a_sheet_takes_its_restyle_with_it` covers a restyle already finished, never one still running. | Put `delete_sheet` under the same per-asset lock its siblings use, and refuse the delete while a `pixel_sheet` row targeting that `sheet_id` is queued or running. Regression: `test_deleting_a_sheet_while_its_restyle_is_running_does_not_resurrect_it`. |
| **create2-03 — Clay viewport: element drag** | ~~**A multi-object element drag costs one Ctrl+Z per object.**~~ **Built 2026-09-06:** `_commit_element_drag`'s loop is wrapped in `history.mark()`/`collapse_since()`, and the docstring's "One `MeshEdit` per object" — the rejected reading, not the rule — is gone. Test: `test_a_multiobject_element_drag_is_one_undo_step`, which failed before the fix with `assert 6 == (3 + 1)`. Original finding: [_view_drag.py:966](../src/warlock/studio/_view_drag.py) commits one `doc.set_mesh` per dragged object with no `history.mark()`/`collapse_since()` wrap. *Reproduced* (`probes/create2-e-01.py`, re-run by the orchestrator: "element drag across 2 objects: history advanced by 2 step(s)"). The sibling object path at [_view_drag.py:760](../src/warlock/studio/_view_drag.py) carries a docstring that reverses exactly this reading — "This reverses the earlier 'one history step per object'… some limbs moved and some not, a document the user never made" — and `_commit_element_drag` still says "One `MeshEdit` per object". `tests/test_clay_history.py` covers only `_commit_drag`; the element path was never re-checked. | Wrap the loop in the `mark()`/`collapse_since()` idiom the object path already uses. Regression: `test_a_multiobject_element_drag_is_one_undo_step`. |

## Medium

| ID / where | Finding and evidence | Recommended fix |
|---|---|---|
| **create2-04 — Create: recipe column** | ~~**A live negative prompt is greyed out with a reason that is not true of it.**~~ **Built 2026-09-06:** `negative_prompt_note` resolves the recipe and reads the capability; `_negative_supported` is now defined as `negative_prompt_note(...) is None`, so the gate and the note cannot disagree by construction. Test: `test_negative_prompt_note_follows_the_resolved_tier`. The orchestrator confirmed against `git show HEAD:` that the pre-fix function read only `form["base_model"]` and had no path to `None` for that form. Original finding: [settings_2d.py:1887](../src/warlock/studio/panes/settings_2d.py) gates the section on the *resolved* recipe, but the note it draws inside, [`negative_prompt_note`:1463](../src/warlock/studio/panes/settings_2d.py), still reads the raw persisted `form["base_model"]` — which `_model`'s `else` branch at :1719 never clears when the user leaves Advanced. *Inspected.* Under Automatic routing to a full-CFG tier, the section opens (correctly — `generate()` honours the field at :2697) while the note says "This model runs at guidance 0, so the negative prompt has no effect" and disables it. Its two siblings, `img2img_note` and `recipe_structure_note`, were both re-worked to read the resolved recipe for this precise reason; this one was left behind. `tests/test_settings_2d_notes.py` only calls it with a fabricated `base_model`, never under `model_mode="auto"`. | Resolve the recipe and read the capability, falling back to the raw field only under Advanced, so the note and the section's gate cannot disagree. Regression: `test_negative_prompt_note_follows_the_resolved_tier`. |
| **create2-05 — Create: 3D upload** | ~~**A custom triangle budget is honoured for a promoted reference and dropped for an uploaded one.**~~ **Built 2026-09-06:** `_upload_kwargs` sends `custom_triangles` alongside `profile`, mirroring `promote_kwargs`. Test: `test_upload_kwargs_sends_custom_triangles_when_profile_is_custom`, covering both `upload()` and `upload_bytes()`, which failed before the fix with `KeyError: 'custom_triangles'`. Original finding: [settings_3d.py:778](../src/warlock/studio/panes/settings_3d.py) sends `profile` without `custom_triangles`, unlike `promote_kwargs` at :519 which sends both. *Reproduced* (`probes/create2-b-01.py`). With `profile == "custom"` the door gets `custom=None` and refuses — on the one path this module's own docstring says exists so that "a form field cannot be honoured for a dropped file and quietly ignored for a rendered one". Neither `tests/test_upload_pane.py` nor `tests/test_create_stage_controls.py` exercises `profile == "custom"` through either upload site. | Send `custom_triangles` alongside `profile`, mirroring `promote_kwargs`. Regression: `test_upload_kwargs_sends_custom_triangles_when_profile_is_custom`. |
| **create2-06 — Create: sheet panel** | ~~**The sheet cap is the one refusal the panel does not state before the press.**~~ **Built 2026-09-06:** `sheet_panel.sheet_cap_reason` states it, reading `ctx.state.preview` and `ctx.cache.jobs` — no new frame-thread query — with `MAX_SHEETS` imported from `rigging`, never retyped. Test: `test_sheet_panel_states_the_sheet_cap_before_the_button_is_pressed`, which failed before the fix with `AttributeError: no attribute 'sheet_cap_reason'`. Original finding: [sheet_panel.py:331](../src/warlock/studio/panes/sheet_panel.py) lists every refusal `create_sheet` can raise except `MAX_SHEETS`; the button stays enabled and [sheets.py:85](../src/warlock/service/sheets.py) raises a fieldless `Conflict` after the press. *Inspected.* A user iterating poses and clips for one character reaches the cap mid-press with no indication of how close they were. `sheet_panel.validate` has no test caller at all. | State the cap in the problems list before the button is drawn, from the count the pane already holds. Regression: `test_sheet_panel_states_the_sheet_cap_before_the_button_is_pressed`. |
| **create2-07 — Create: generation request** | ~~**A string-typed numeric in a sub-document turns a refusal into a `TypeError`.**~~ **Built 2026-09-06:** `from_dict` now coerces every field of all three sub-documents (`tile`, `sprite`, `model` — the probes only demonstrated the first two), and a value that cannot be coerced is left unconverted and reported as a `CompatibilityIssue` rather than folded to a default that would hide it. Tests: four in the new `tests/test_generation_from_dict.py`, three of which failed before the fix with the pasted `TypeError`. Original finding: [generation.py:227](../src/warlock/generation.py) casts every top-level scalar with `str()`/`int()`/`float()` but passes the `tile`, `sprite` and `model` sub-dicts through raw, so `{"variants": "2"}` or `{"candidate_count": "1"}` survives construction and `validate_request` raises at [:718](../src/warlock/generation.py) / [:762](../src/warlock/generation.py). *Reproduced* (`probes/create2-f-01.py` and `-f-02.py`, both re-run by the orchestrator: `'<=' not supported between instances of 'int' and 'str'`). The module's docstring offers itself to settings migration and worker planning, and `service.jobs.create_generation_request` calls `validate_request` unguarded — but that adapter has no caller in `src/`, so nothing reaches it today. `from_dict` has no test by name anywhere. | Coerce the sub-document fields the way the top-level fields already are. Regression: `test_from_dict_coerces_string_typed_tile_and_sprite_numerics`. |

## Low

| ID / where | Finding and evidence | Recommended fix |
|---|---|---|
| **create2-08 — Create: sprite panel** | ~~**The draft cap is not stated before the press either.**~~ **Built 2026-09-06:** `sprite_panel.sprite_draft_cap_reason` states it, reusing the stamp-cached `draft_records` the frame already pays for, with `MAX_SPRITE_DRAFTS` imported from `rigging`. Test: `test_sprite_panel_states_the_draft_cap_before_the_button_is_pressed`, which failed before the fix with `AttributeError`. Original finding: [sprite_panel.py:201](../src/warlock/studio/panes/sprite_panel.py) grades the button on `busy`/`locked`/`drawable` alone and never reads the `MAX_SPRITE_DRAFTS` cap enforced at [sprites.py:632](../src/warlock/service/sprites.py). *Inspected.* The manual states the limit of 50 as a real one; the 51st draft is refused by a bare `Conflict` after the press. Same shape as create2-06, one file over. | State it before the press, from the draft count the pane can already read. Regression: `test_sprite_panel_states_the_draft_cap_before_the_button_is_pressed`. |
| **create2-09 — Clay viewport: keyboard drag** | ~~**Switching a keyboard drag's kind mid-gesture leaves the mesh drawn where it is not.**~~ **Built 2026-09-06:** `_restart_keyboard_drag` now clears each live element drag's preview and re-runs `_preview_positions` from the mesh's pre-drag positions, so the buffer matches the restored overlay the same frame. Test: `test_switching_keyboard_drag_kind_resets_the_element_gpu_buffer` in the new `tests/test_view_drag_preview.py`, which failed before the fix with `assert array([[4.5, 4.5, 4.5], ...]) is None`. Original finding: [_view_drag.py:587](../src/warlock/studio/_view_drag.py) restores the selection overlay but rebinds only `translation`/`rotation`/`scale`, which an element drag never touches, so the GPU vertex buffer written by `_preview_positions` keeps the abandoned transform until the next mouse-move. *Inspected.* For one or more frames the mesh and its own overlay disagree; the next frame corrects it and nothing is lost. | Re-run the element preview for every dragged uid when the drag kind changes in an element mode. Regression: `test_switching_keyboard_drag_kind_resets_the_element_gpu_buffer`. |

## Human-only — owed, not open

None.

| ID | What has to happen | Who / what it needs |
|---|---|---|
| — | — | — |

## Fix phase — 2026-09-06

All nine findings were closed the day they were filed, by seven file-disjoint fixers. Each
wrote its regression first and proved it failed against the unfixed code; the failing
assertions are quoted in the rows above. One fixer argued its failure from the pre-fix
source rather than running it — the orchestrator checked that claim against
`git show HEAD:` and accepted it. Six INVARIANTS paragraphs were returned by fixers and
written by the orchestrator, along with the manual line and the CHANGELOG entry. The gate
was then run once, with nothing else editing: `uv run ruff check .` clean,
`uv run pytest` **18444 passed, 45 skipped** in 139 s, `uv run python scripts/preflight.py
--fast` passed — green on the first run, with no round-trip back to any fixer. Nothing
moved to `TODO.md`: this run found no human-only work.

Two things fixers reported that were outside their findings and are **not** closed:
`_upload_kwargs` also sends `rig` only when truthy where `promote_kwargs` always sends it
as an explicit boolean (harmless today, because an upload starts a fresh job with no
inherited rig to clear); and `sheet_panel`'s confirm dialog surfaces the new delete
`Conflict` as a generic toast rather than a written message.

## Coverage

- Six explorers ran, one per file group. Five read their whole assignment end to end:
  `settings_2d.py` (2735); `settings_3d.py` + `settings_character.py`; `sheet_panel`,
  `sprite_panel`, `pose_panel`, `stage_rig`; all nine viewport-interaction modules
  including `_view_drag.py` (1146); and `generation`, `guidance`, `judge`, `sweep`,
  `provenance` plus `service/judge.py`.
- **Still unread after this pass:** the viewer geometry explorer covered `gltf.py`,
  `scene.py`, `glbwrite.py`, `capture.py`, `env.py`, `render.py` and `bonelines.py` in
  full but ran out of budget before `programs.py`, `math3d.py`, `sheet.py`, `glctx.py`,
  `grid.py` and `markers.py` — about 1,220 lines. Nothing in those six is claimed clean;
  they are unaudited. `_viewer_pose.py` is also unread: it is imported by `viewer_embed.py`
  but was on no explorer's list.
- Checklist items not evaluable in this scope: 15 (subprocess), 17 (cancel) and 18
  (recovery) have no code in these files; 5 (docs-vs-code) was walked against manual
  chapters 12, 22, 23, 24, 27 and 33 but not line-by-line against every claim in them.
- Four findings this run carry probes under `scratchpad/audit-2026-09-06/probes/`; the
  orchestrator re-ran all four rather than taking the reports on trust. They are outside
  the repository and will not survive the session — a fixer must re-derive each as a
  regression test in `tests/`.
- Not done by this audit: no interactive pass, no fresh install, no GPU inference, no
  third-party editor validation, no dependency-advisory scan.
