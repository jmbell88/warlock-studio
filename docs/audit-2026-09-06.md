# Warlock Studio audit — 2026-09-06

**Recommendation:** all four findings were built on 2026-09-06 and are struck below. What is
left owed is coverage, not defects: re-run this audit over the parts of the row named under
Coverage, which this pass did not read.

**4 findings: 0 Critical, 1 High, 2 Medium, 1 Low; 0 human-only. All four closed 2026-09-06.**

Audited: version **0.0.37**, working tree at **`daa197b8`**. Dirty at start:

```
 M TODO.md
 M docs/manual/29-inker-animation.md
 M src/warlock/studio/inker_keys.py
 M src/warlock/studio/inker_ops.py
 M src/warlock/studio/inker_state.py
 M src/warlock/studio/manual/targets.py
 M src/warlock/studio/panes/inker_canvas.py
 M src/warlock/studio/panes/inker_preview.py
 M src/warlock/studio/panes/inker_textures.py
 M src/warlock/studio/skeletons.py
 M tests/inker/test_inker_ux.py
 M tests/test_layout.py
 M tests/test_layouts.py
 M uv.lock
?? src/warlock/studio/inker/walk/
?? src/warlock/studio/inker_walk.py
?? src/warlock/studio/panes/inker_walk.py
?? src/warlock/studio/panes/inker_walk_canvas.py
?? tests/inker/test_walk_session.py
?? tests/inker/walk/
```

None of the dirty paths is in this slice; they are in-progress Inker walk-cycle work and
were not read as part of the audit. Scope: `create` → slice `create`.
`uv run ruff check .`: All checks passed. The suite was not run by this audit; findings are
from reading. No probe was written: every finding below is *Inspected*, so the probes
directory is empty.

**Vocabulary.** *Critical*: crash, data loss, or corrupts a document. *High*: a wrong
result the user will hit, or a promise in the docs the code breaks. *Medium*: a defect
with a workaround, or a hard-invariant violation not yet reachable. *Low*: hardening,
consistency, naming. *Reproduced*: an executable example confirmed it. *Inspected*:
follows from the current code path. *Evidence gap*: release proof is missing from the
repository, not a demonstrated failure.

**Second-look.** The one High below was re-read by the orchestrator against the working
tree: `texture_panel.py` was confirmed to contain no `forms.Form`, no `errors=`, no
`clear_field_errors`, while `remesh_panel.py` and `retarget_panel.py` each carry all
three, and `_jobs_rework.retexture_job` was confirmed to raise with `field="strength"`,
`"control"`, `"control_scale"` and `"texture_size"` against controls that pane draws.
Downgraded during merge: **create-03** (reported as High, "an upgraded user's Create form
can open as the wrong asset type"), because `create_assets.selected()` already prefers
`asset_type` over `generation_type` and `sync_legacy_fields` rewrites both on the first
Create draw, so no wrong door survives to a submit; what remains is a migration that
contradicts its own comment, which is Medium.

## Critical

None.

## High

| ID / where | Finding and evidence | Recommended fix |
|---|---|---|
| **create-01 — Create: re-texture panel** | ~~**A re-texture refusal rings no control.**~~ [panes/texture_panel.py:39-211](../src/warlock/studio/panes/texture_panel.py) draws `prompt`, `strength` and `control_scale`, and [service/_jobs_rework.py:216-254](../src/warlock/service/_jobs_rework.py) refuses each of them by `field` name — but the pane never wraps its controls in `forms.Form(errors=ctx.state.field_errors, on_edit=ctx.state.clear_field_error)` and never calls `ctx.state.clear_field_errors()` on submit, unlike both sibling rework panes. *Inspected.* The refusal reaches the user as a bare toast with nothing marked, and a previous refusal's rings are never cleared; this is the defect `tests/test_field_error_wiring.py` was written for after it was found in `retarget_panel.py`, and that file's `FIELD_FORMS`, `ELSEWHERE` and `cases` tables do not name `texture_panel.py`, so its sweep cannot see the gap. Most reachable via a reroll that re-submits a stored `strength`/`control_scale` predating a tightened `models.RETEXTURE_STRENGTH_*`/`CONTROL_SCALE_*` range, which [_jobs_resubmit.py:339-361](../src/warlock/service/_jobs_resubmit.py) does not re-check. **Built 2026-09-06:** the pane's controls now open `forms.Form("retexture-settings", errors=..., on_edit=...)`, `_submit` clears the rings on each press, and `texture_panel.py` was added to `FIELD_FORMS` so the sweep covers it from now on. Test: `test_a_retexture_refusal_rings_the_control_on_texture_panel`, which failed before the fix with `texture_panel never opens forms.Form('retexture-settings')`. | Wrap the pane's controls in `forms.Form("retexture-settings", errors=ctx.state.field_errors, on_edit=ctx.state.clear_field_error)`, clear the rings at the top of `_submit`'s pressed branch, and add the pane to `FIELD_FORMS` so the sweep covers it from now on. Regression: `test_a_retexture_refusal_rings_the_control_on_texture_panel`. |

## Medium

| ID / where | Finding and evidence | Recommended fix |
|---|---|---|
| **create-02 — Create: the three rework panels** | ~~**A collision with an in-flight rework is discovered by pressing the button.**~~ [panes/retarget_panel.py:186-217](../src/warlock/studio/panes/retarget_panel.py), [remesh_panel.py:158-176](../src/warlock/studio/panes/remesh_panel.py) and [texture_panel.py:185-201](../src/warlock/studio/panes/texture_panel.py) grey their submit only on their own client-side busy flag, never on `_jobs_lifecycle.dependent_jobs`, so with a sibling rework already queued against the same asset the button stays live and the `Conflict` arrives after the press. *Inspected.* Manual chapter 23 frames these refusals as things the panel will not hide, and shows the stale-rig warning *before* the button; this one is only visible after a wasted submit. **Built 2026-09-06:** each of the three panels grew a pure `dependent_job_reason()` beside its draw call, read from the frame's `jobs_cache` window rather than the store, and greys the button with that reason. Tests: `tests/test_rework_panel_conflicts.py`, five cases; `test_retarget_panel_greys_rebuild_button_while_a_dependent_retexture_is_running` failed before the fix with `no attribute 'dependent_job_reason'`. Manual chapter 23 gained the sentence. | Read the dependent-job count before drawing the button and disable it with a reason naming the job in flight, in the place the stale-rig warning already occupies. Regression: `test_retarget_panel_greys_rebuild_button_while_a_dependent_retexture_is_running`. |
| **create-03 — Create: settings migration** | ~~**The `asset_type` migration re-derives `generation_type` from the stale field it is replacing.**~~ [studio/settings.py:79-94](../src/warlock/studio/settings.py) sets `form["asset_type"]` from the legacy switches, then sets `form["generation_type"] = create_assets.asset_type_from_params(form)` — and [create_assets.py:166-172](../src/warlock/studio/create_assets.py) checks `generation_type` *first*, returning the pre-migration value in preference to the field just migrated. *Inspected.* Sharpest in the corrupt-value branch, whose own comment says a bad `asset_type` "must not resurrect a contradictory legacy selection" and which then writes back exactly that contradiction. The divergence is repaired by `sync_legacy_fields` on the first Create draw, so no wrong door is reachable today; it is a stored state that contradicts the invariant that the two fields are one choice. **Built 2026-09-06:** `_migrate` now assigns `form["generation_type"] = form["asset_type"]` directly. Test: `test_migrate_makes_the_two_asset_type_fields_agree_on_a_pre_asset_type_file`, which failed before the fix with `assert '3d_model' == 'seamless_material'`. | Set `form["generation_type"] = form["asset_type"]` directly, as `state.restore_form` already does, instead of routing through the reader that prefers the stale field. Regression: `test_migrate_makes_the_two_asset_type_fields_agree_on_a_pre_asset_type_file`. |

## Low

| ID / where | Finding and evidence | Recommended fix |
|---|---|---|
| **create-04 — Create: matte preview** | ~~**A failed task is matched to the open preview by suffix, not identity.**~~ [studio/matte_preview.py:186](../src/warlock/studio/matte_preview.py) tests `str(done.key).endswith(state.job_id)` rather than `done.key == matte_preview.key(state.job_id)`; job ids are `uuid4().hex[:12]`, so a key ending in another job's id would latch that job's failure onto the reference on screen. *Inspected.* A wrongly-latched `failed_stamp` stops `pump` re-submitting, leaving the cutout modal up with no toast and no retry until the file's mtime moves. `tests/test_matte_handoff.py::test_a_failure_for_another_reference_does_not_latch_this_one` covers a non-matching key, not a suffix-colliding one. **Built 2026-09-06:** the comparison is now `getattr(done, "key", None) != key(state.job_id)`; the fixer confirmed by grep that `pump` is the only submit site and prefixes nothing, so exact equality breaks no handoff. Test: `test_a_failure_key_that_merely_ends_with_the_open_job_id_does_not_latch_it`, which failed before the fix with `assert 1788701115489245600 is None`. | Compare the full key for equality. Regression: `test_a_failure_key_that_merely_ends_with_the_open_job_id_does_not_latch_it`. |

## Human-only — owed, not open

None. Nothing in this slice needed a card, a clean machine, a third-party editor or ears
to settle.

| ID | What has to happen | Who / what it needs |
|---|---|---|
| — | — | — |

## Coverage

- Slices run: `create`. Slices skipped: none in scope.
- Explorers: three ran over the row with split emphases (threads/undo/stale-completion/
  cancel; writes/ceilings/sweeps/presence/subprocess/recovery; greyed reasons/docs-vs-code/
  layering/blind spots/inventories). None failed or timed out, but all three stopped short
  of the row's largest files. **Not read in this pass, and therefore not audited:**
  `panes/settings_2d.py` (only `generate`/`submit_job`/`validate` were read),
  `panes/settings_3d.py`, `panes/settings_character.py`, `panes/sheet_panel.py`,
  `panes/sprite_panel.py`, `panes/stage_rig.py` and `panes/pose_panel.py` bodies; all of
  `studio/viewer/` except `camera.py` and the ceiling section of `gltf.py`;
  `studio/viewer_embed.py` and every `_view_*.py`; and the bodies of `generation.py`,
  `guidance.py`, `judge.py`, `sweep.py` and `provenance.py` (import-checked for layering
  only, which was clean). That is most of the row by line count; the four findings should
  be read as a floor, not a total.
- Checklist items not evaluable in this scope: item 1 (undo) — Create has no undo surface
  of its own; item 6 (per-frame recompute) and item 17 (cancel) were spot-checked in the
  files that were opened and not walked across the unread panes above; items 9–12, 14, 15
  and 18 were walked only over the headless modules, not the pane set.
- Verified as already correct, so not reported: the base-model registry is eleven entries,
  matching manual chapter 12; the prompt-history cap is 20 in both code and manual; the
  five top-level Create modules import no imgui, moderngl or pygame.
- Not done by this audit: no interactive pass, no fresh install, no GPU inference, no
  third-party editor validation, no dependency-advisory scan. A passing suite does not
  close those.

## Fix phase — 2026-09-06

All four findings were closed the day they were filed, by three file-disjoint fixers. Each
wrote its regression first and proved it failed against the unfixed code; the failing
assertions are quoted in the rows above. The gate was then run once, with nothing else
editing: `uv run ruff check .` clean, `uv run pytest` **18427 passed, 45 skipped** in 140 s,
`uv run python scripts/preflight.py --fast` passed. One ruff E501 surfaced on the first gate
run — the create-03 regression's name was 110 characters — and the name was shortened;
nothing else in the gate needed a second round. Nothing moved to `TODO.md`: this audit found
no human-only work.

## How this file is closed

A finding is struck the day it is built: `~~claim~~ Built YYYY-MM-DD: what was done, and
the test.` A human-only finding is moved to `TODO.md`. When nothing is left, this file is
deleted rather than ticked; `git log --diff-filter=D -- docs/audit-*.md` finds it.
Nothing under `src/` or `scripts/` may cite this filename.
