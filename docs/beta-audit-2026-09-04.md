# Warlock Studio private beta audit — 2026-09-04

**Recommendation: resolve the High findings before distributing the beta.** There are **26 findings: 0 Critical, 3 High, 16 Medium, 7 Low**. Several Medium findings affect authored work, exported assets, and ordinary Muse playback; prioritize those next.

Audited version: **0.0.32**, current source at **c64957c5**. The installer/pack changes were uncommitted when the audit started and were committed during the review. The findings were checked against the resulting source. No application source was changed by this audit.

“Page” below means the affected Studio screen or documentation page. The linked source locations provide exact line numbers; Markdown documentation has no fixed printed pagination. **Reproduced** means a small executable example confirmed the behavior. **Inspected** means the finding follows from the current code path. **Evidence gap** means release proof is missing from the repository, not that the feature has been demonstrated to fail.

Validation completed:

- Default suite: **16,392 passed, 41 skipped**, 150.39 seconds.
- Serial performance lane: **20 passed, 2 skipped**, 21.01 seconds.
- Ruff and `scripts/preflight.py --fast`: passed, version lockstep 0.0.32.
- Offline, isolated reproductions: [probe script](D:/Projects/Warlock/scratch/beta_audit_probe.py) and [recorded results](D:/Projects/Warlock/scratch/beta_audit_probe.json). Run with `uv run --no-sync python scratch/beta_audit_probe.py`.

Scope: source review of startup/shutdown, optional packs, installer and CI, file parsing and persistence, editor undo, Muse playback/export, Troupe scoring, and user/release documentation, supported by the repository-wide default tests. This is not a claim of exhaustive verification of every control or algorithm. No interactive visual/accessibility pass, fresh VM installation, new installer build, live model download, real GPU inference, third-party editor validation, native binary reverse engineering, or current dependency-advisory scan was performed. Passing tests do not resolve those gaps.

**Critical — none confirmed**

No demonstrated remote code execution, systemic irreversible data loss, or equivalent Critical defect was established within this scope. This is not a security certification.

**High — address before distribution**

| ID / page | Finding and evidence | Recommended fix |
|---|---|---|
| **H01 — Clay: Import GLB; 3D viewer** | **A small GLB can drive unbounded aggregate allocations in the application process.** [gltf.py:325](D:/Projects/Warlock/src/warlock/studio/viewer/gltf.py:325), [gltf.py:398](D:/Projects/Warlock/src/warlock/studio/viewer/gltf.py:398), [gltf.py:466](D:/Projects/Warlock/src/warlock/studio/viewer/gltf.py:466). Accessors have a 256 MiB individual ceiling, but meshes/primitives have no shared allocation budget; repeated references produce separate position and index arrays. **Reproduced:** a 4,036-byte, bufferless GLB created 6,144,000 bytes of geometry across 128 separate arrays. Increasing counts/repetitions can exhaust host memory before refusal. Clay checks its triangle limit only **after** this load: [glbimport.py:103](D:/Projects/Warlock/src/warlock/studio/clay/glbimport.py:103). | Preflight total decoded geometry, textures, and primitive counts before allocation. Enforce a document-wide memory budget, including dtype conversions and generated indices. Cache/share immutable accessor results where possible. Test repeated zero-filled accessors and repeated images without allocating dangerous amounts of RAM. |
| **H02 — Settings → Packs; Quit/window close** | **Normal Quit bypasses the pack install's “cannot be interrupted safely” protection.** [main.py:3101](D:/Projects/Warlock/src/warlock/studio/main.py:3101) recognizes downloads and exports, but not `pack:` tasks. [tasks.py:277](D:/Projects/Warlock/src/warlock/studio/tasks.py:277) terminates tracked children after the shutdown grace period. Meanwhile [app_settings.py:1712](D:/Projects/Warlock/src/warlock/studio/panes/app_settings.py:1712) intentionally removes Cancel during installation. **Reproduced:** a running `pack:music` task produces an empty quit summary. A sufficiently long pip install can therefore be interrupted while writing the live runtime. | Track a real installation phase, guard every normal quit route, and defer shutdown until the non-cancellable commit phase completes. Permit download cancellation through an explicit worker acknowledgement rather than a sampled progress percentage. Add recovery for interrupted installs; pair this with M01. |
| **H03 — Installer / first launch / Settings → Packs** | **The new base-only distribution lacks recorded end-to-end release proof.** [build.ps1:162](D:/Projects/Warlock/installer/build.ps1:162) prunes heavy dependencies; its smoke checks import Studio and inspect the manifest at [build.ps1:297](D:/Projects/Warlock/installer/build.ps1:297). They do not install a pack into a fresh base runtime and use it. [TODO.md:40](D:/Projects/Warlock/TODO.md:40) records the outstanding non-developer-machine install, and [TODO.md:604](D:/Projects/Warlock/TODO.md:604) says only the rig pack has been collected. **Evidence gap:** the earlier full-runtime installer run does not validate this changed distribution. | Build the exact release candidate, install on clean Windows without development Python/CUDA tools, launch, install each pack, fetch models, and exercise at least one reference, rig, and music job. Check cancel/retry, restart, upgrade, uninstall, and reinstall with user data retained. Record the installer hash, actual sizes, hardware, and results. |

**Medium — functional defects and release gaps**

| ID / page | Finding and evidence | Recommended fix |
|---|---|---|
| **M01 — Settings → Packs: completion and retry** | **“Installed” does not prove usability, and retry cannot repair some damaged distributions.** [pack_worker.py:272](D:/Projects/Warlock/src/warlock/pipelines/pack_worker.py:272) uses `find_spec`, which locates a module without importing it. [packs.py:519](D:/Projects/Warlock/src/warlock/packs.py:519) skips matching distribution metadata; [service/packs.py:257](D:/Projects/Warlock/src/warlock/service/packs.py:257) returns success when nothing is pending, without probing. **Reproduced:** verification accepts a module whose import immediately raises `ImportError`; matching metadata alone yields zero repair candidates. This does not assert that a normal clean installation fails. | Run actual dependency smoke imports in a fresh disposable child after installation. Validate a completion receipt/file set, and provide Repair that reinstalls damaged wheels at the pinned versions. Probe even on the “already installed” path. |
| **M02 — Installer: upgrade** | **Upgrading removes all user-installed packs.** [warlock.iss:53](D:/Projects/Warlock/installer/warlock.iss:53) deletes `python\Lib\site-packages`; [build.ps1:162](D:/Projects/Warlock/installer/build.ps1:162) stages only the base. **Inspected:** an upgrade from a configured installation leaves Create/Muse/rigging dependencies absent, with no saved selection or automatic restoration. Cached wheels reduce download cost but do not restore installed functionality. | Persist the user's chosen packs outside the runtime. Reinstall compatible pinned packs as part of upgrade recovery, or provide an explicit post-upgrade Restore packs workflow. Do not simply preserve old site-packages across incompatible releases. Test configured-version → upgraded-version behavior. |
| **M03 — Save/export across editor tabs** | **Concurrent writers to the same destination share one temporary filename.** [atomic.py:60](D:/Projects/Warlock/src/warlock/studio/atomic.py:60) and [atomic.py:89](D:/Projects/Warlock/src/warlock/studio/atomic.py:89) use `.name.tmp`. Export task keys can differ by tab: [packwright_io.py:277](D:/Projects/Warlock/src/warlock/studio/packwright_io.py:277). **Reproduced:** overlapping staging contexts use the same file; the later writer publishes and the earlier one fails with `FileNotFoundError`. Other interleavings can publish another writer's bytes. | Use unique temporary siblings for every operation and serialize writes by normalized destination/set of destinations. Add an overlapping-export test proving that one writer cannot rename or remove another's staging file. |
| **M04 — Settings → Models / Doctor** | **Zero-byte weights are reported healthy for the engine and most optional models.** [doctor.py:418](D:/Projects/Warlock/src/warlock/doctor.py:418), [doctor.py:765](D:/Projects/Warlock/src/warlock/doctor.py:765), [doctor.py:864](D:/Projects/Warlock/src/warlock/doctor.py:864), [doctor.py:953](D:/Projects/Warlock/src/warlock/doctor.py:953). These branches use presence checks without the base branch's suspect-file check. **Reproduced:** all ten engine probe files were empty, `suspect_files` identified all ten, and Doctor still returned OK. The engine weights row is now nonfatal; the prior TODO description calling it fatal is stale. | Apply one shared, cheap usability check consistently to engine, LoRA, adapter, control, metric, music, separation, pose, and matting rows. Surface damaged files with a repair/download action. Add zero-byte coverage per model kind. |
| **M05 — Inker: layer opacity** | **An interrupted opacity gesture can leave a visual edit without a history/dirty transition.** [inker_menu.py:384](D:/Projects/Warlock/src/warlock/studio/panes/inker_menu.py:384) directly changes opacity and only records the edit on `is_item_deactivated_after_edit()`. **Inspected:** if the control disappears before that release is rendered, the old value stays in the module dictionary and the undo step is never pushed. This can also contaminate a later gesture's starting value. | Use the common gesture-folding mechanism, with document/layer ownership and explicit interruption finalization. Ensure live changes participate in dirty/recovery tracking. Test switching away or closing the control during a drag, followed by save/undo. |
| **M06 — Plotter layer opacity and Terrain; Packwright settings** | **Several continuous edits still push history once per value-change frame.** [plotter_layers.py:208](D:/Projects/Warlock/src/warlock/studio/panes/plotter_layers.py:208); Packwright Columns, Padding, and Extrude at [packwright_settings.py:88](D:/Projects/Warlock/src/warlock/studio/panes/packwright_settings.py:88), [packwright_settings.py:121](D:/Projects/Warlock/src/warlock/studio/panes/packwright_settings.py:121); Wang colour/probability at [plotter_tileset_editor.py:1028](D:/Projects/Warlock/src/warlock/studio/panes/plotter_tileset_editor.py:1028). Each reaches an unconditional history push for a changed value, e.g. [packwright/document.py:410](D:/Projects/Warlock/src/warlock/studio/packwright/document.py:410). **Inspected:** one gesture needs many Undo presses and consumes history unnecessarily. | Wrap each gesture with `controls.fold_undo`; cover Columns as well as the two sliders. Test colour-picker component drags, numeric editing, interruption, and one Undo returning to the pre-gesture state. |
| **M07 — Muse: Stop / Play** | **Playing a loaded take again resets loop markers, crossfade, and position.** [muse_player.py:219](D:/Projects/Warlock/src/warlock/studio/panes/muse_player.py:219) seeks, then calls the disk-loading `play`; [muse_mode.py:254](D:/Projects/Warlock/src/warlock/studio/muse_mode.py:254) replaces `Player`. **Reproduced:** a 2–8 s region, 200 ms fade, and 5 s offset became no region, 40 ms fade, and zero offset. Additionally Stop does not capture the current mixer position before stopping. | Resume the existing player's PCM for the same take; capture the current position before stopping. Replace state only for an intentional different-take load. Test the full Stop → Play control sequence. |
| **M08 — Muse: Export the loop** | **Crossfade blends the wrong boundary and can introduce a click into an already seamless loop.** [loops.py:354](D:/Projects/Warlock/src/warlock/studio/muse/loops.py:354) mixes samples before `start` into the beginning of the loop, without making the end of the repeated body meet that new beginning. **Reproduced:** a constant +20,000 loop with -20,000 audio before it had a zero original boundary jump and a 40,000-unit jump after crossfade. | Implement a wrap-aware end-to-start crossfade with an explicit duration policy. Test the repeated output's actual seam, constant/plateau signals, stereo signals, start=0, and short loops. Listen to repeated exported files. |
| **M09 — Muse: Play the loop / Crossfade** | **Audition does not play the crossfaded audio that export writes.** [muse_mode.py:389](D:/Projects/Warlock/src/warlock/studio/muse_mode.py:389) passes a raw PCM slice to the mixer; [muse_io.py:133](D:/Projects/Warlock/src/warlock/studio/muse_io.py:133) applies crossfade when exporting. **Reproduced:** audition was byte-identical to the untreated slice and different from the export calculation. Changing the crossfade control cannot be judged through the advertised audition. | Build one loop buffer using the corrected M08 algorithm and use it for both audition and export. Cache it by take/region/fade and refresh playback deliberately when those settings change. |
| **M10 — Muse: seeking during loop playback** | **Seeking into a loop repeats only the remaining tail.** [muse_mode.py:387](D:/Projects/Warlock/src/warlock/studio/muse_mode.py:387) slices from the requested position to `loop_end` and sets `loops=-1` on that slice. **Reproduced:** seeking to 5 s in a 2–8 s region repeated 3 seconds rather than the 6-second region. The displayed markers no longer describe playback. | Play the remainder once, then repeat the full region, or use a loop buffer/offset implementation that preserves the original boundaries. Test seeks before, within, and after the region, plus changing/clearing markers while sounding. |
| **M11 — Muse: quickly auditioning takes / Stop while loading** | **Late decode results can override the user's latest playback choice.** [muse_mode.py:228](D:/Projects/Warlock/src/warlock/studio/muse_mode.py:228) submits per-job keys; [muse_mode.py:248](D:/Projects/Warlock/src/warlock/studio/muse_mode.py:248) adopts every successful load and starts playback. No request generation or cancellation token ties results to the latest intent. **Reproduced:** B completes, then an older A completes, and A replaces the player. Stop likewise does not invalidate an outstanding decode. | Record a monotonically increasing audition request token. Ignore stale completions and invalidate pending playback intent on Stop or relevant navigation. Test out-of-order A/B completion and Stop-before-decode-completes. |
| **M12 — Packwright: re-exporting an atlas** | **An obsolete `.tsx` can remain beside a replaced atlas.** [packwright_io.py:261](D:/Projects/Warlock/src/warlock/studio/packwright_io.py:261) only writes TSX for an eligible grid; it neither resolves an existing TSX when switching to MaxRects nor when grid TSX generation is refused. **Inspected:** exporting a grid then exporting an incompatible layout under the same basename leaves the old tileset referencing the new PNG. | Detect collisions with previous sidecars. After successful publication, retire only known-owned obsolete sidecars, or refuse/report the stale TSX and require a different basename. Test grid → MaxRects and grid → unsupported-columns re-export. |
| **M13 — Troupe: QA / worst-frame navigation** | **A blank frame loses priority to a minor later warning.** [qa.py:313](D:/Projects/Warlock/src/warlock/studio/troupe/qa.py:313) sets the blank result only when `worst` is empty and does not increase `worst_ratio`. **Reproduced:** a blank first cell was outranked by a later centroid jitter of 0.0625, below its 0.15 bad threshold. | Rank severity before metric magnitude, with blank frames explicitly at the intended severity. Test mixed blank and warning-only frames in both orders, rather than merely testing that blank is flagged. |
| **M14 — Inker/Aseprite and Plotter/Tiled export** | **Interop coverage is mostly self-generated rather than independently validated.** [COMPAT.md:11](D:/Projects/Warlock/docs/COMPAT.md:11), [COMPAT.md:100](D:/Projects/Warlock/docs/COMPAT.md:100), [TODO.md:145](D:/Projects/Warlock/TODO.md:145). **Evidence gap:** Aseprite output lacks recorded real-app confirmation; many Tiled constructs lack fixtures from Tiled. Basic orthogonal CSV interchange has been exercised externally, so “Tiled has never been tested” would overstate the gap. | Add real-app golden fixtures and verify exports in supported Aseprite/Tiled versions, prioritizing tilemaps, linked cels, transparency, transform flags, multiple tilesets, and infinite maps. Record exact versions and outstanding limitations in beta notes. |
| **M15 — Muse, Sirens, Troupe: hardware-dependent workflows** | **Important advertised workflows still lack recorded production-path evidence.** [TODO.md:132](D:/Projects/Warlock/TODO.md:132), [TODO.md:301](D:/Projects/Warlock/TODO.md:301), [TODO.md:463](D:/Projects/Warlock/TODO.md:463), [TODO.md:497](D:/Projects/Warlock/TODO.md:497) record Troupe end-to-end rendering, audio listening, music/separation cancellation, and memory measurements as outstanding. **Evidence gap:** the 10/12 GiB music and 4/4 GiB separation figures remain estimates; the default suite excludes GPU work. | Run and record these exact workflows on the beta support hardware, including cancellation and the next job afterward. Measure memory peaks at maximum supported durations. Make remaining unvalidated behavior explicit in the invitation/download notes instead of treating the test count as hardware proof. |
| **M16 — INSTALL.md / first-run instructions** | **The primary installation guide still describes the old full-runtime package.** [INSTALL.md:12](D:/Projects/Warlock/INSTALL.md:12), [INSTALL.md:19](D:/Projects/Warlock/INSTALL.md:19), [INSTALL.md:29](D:/Projects/Warlock/INSTALL.md:29) promise a 2.9 GB download and 6.6 GB runtime. Its model setup proceeds without the new dependency-pack step, while the build now prunes those dependencies. **Inspected:** beta testers following this guide can reach downloaded weights without the code needed to use them. | Rewrite the guide around the actual base installer and Settings → Packs → Models workflow. Insert measured sizes from H03, distinguish reusable wheel cache from model storage, and document pack restoration after upgrade. |

**Low — hardening, documentation, and consistency**

| ID / page | Finding and evidence | Recommended fix |
|---|---|---|
| **L01 — Doctor: native tools** | **A directory named like an executable passes the check.** [doctor.py:407](D:/Projects/Warlock/src/warlock/doctor.py:407), [doctor.py:468](D:/Projects/Warlock/src/warlock/doctor.py:468) use `exists()`. **Reproduced** for `trellis-server.exe`. This is a damaged-install diagnostic defect, not evidence of an exploitable execution path. | Use `is_file()` and report an invalid path distinctly. Include a small startup/version probe where appropriate. |
| **L02 — Clay: undo memory accounting** | **Property/material edits have zero accounted cost.** [edits.py:215](D:/Projects/Warlock/src/warlock/studio/clay/edits.py:215), [edits.py:237](D:/Projects/Warlock/src/warlock/studio/clay/edits.py:237), [edits.py:264](D:/Projects/Warlock/src/warlock/studio/clay/edits.py:264) inherit [undo.py:85](D:/Projects/Warlock/src/warlock/studio/undo.py:85). **Inspected:** retained dictionaries/material resources bypass byte-budget eviction, although the depth cap still limits step count. | Account for retained payloads, with deliberate treatment of shared texture buffers. Test representative material/property histories against the budget. |
| **L03 — Service file helper** | **`job_dir_file` relies on its caller to supply a safe name.** [files.py:354](D:/Projects/Warlock/src/warlock/service/files.py:354) validates the job ID but joins an unrestricted name. **Inspected hardening opportunity:** current documented callers use fixed names; no user-controlled traversal exploit was established. | Validate against the intended artifact allowlist, or require a bare leaf name and check containment. Keep this Low unless an untrusted caller is introduced. |
| **L04 — README / manual overview** | **Mode inventories disagree with the rail.** [README.md:81](D:/Projects/Warlock/README.md:81) skips Muse and ends its numbered inventory at twelve even though the opening now correctly says thirteen. [20-overview.md:92](D:/Projects/Warlock/docs/manual/20-overview.md:92) and [20-overview.md:100](D:/Projects/Warlock/docs/manual/20-overview.md:100) list Sirens twice. | Insert Muse before Sirens, remove the duplicate, and fix ordering/numbering. Check inventories against `studio/modes.py`. |
| **L05 — Contributor/setup/model documentation** | **Several setup details are stale or malformed.** [CONTRIBUTING.md:13](D:/Projects/Warlock/CONTRIBUTING.md:13) says three extras beneath a four-extra command; line 17 says roughly 12,000 tests. [39-installation.md:30](D:/Projects/Warlock/docs/manual/39-installation.md:30) omits `music` from its extras table. [MODELS.md:169](D:/Projects/Warlock/docs/MODELS.md:169) opens a code fence that swallows the subsequent rigging heading. | Correct the extras language/table, use an approximate current test count or omit the volatile number, and close the conditioning-model fence before the rigging heading. Render the affected Markdown after editing. |
| **L06 — Third-party notices** | **The model disclosure is internally inconsistent.** [THIRD-PARTY-NOTICES.md:89](D:/Projects/Warlock/THIRD-PARTY-NOTICES.md:89) says every checkpoint comes from Hugging Face, and the table omits Hybrid Demucs, which [MODELS.md:43](D:/Projects/Warlock/docs/MODELS.md:43) already documents separately. This finding concerns inconsistent shipped disclosure; no independent legal determination was made. | Synchronize the notices with the actual model registry and documented source/restrictions. Verify the final disclosure with the appropriate licence reviewer; avoid inferring new permissions from this audit. |
| **L07 — Security policy** | **The policy no longer describes the implemented surfaces.** [SECURITY.md:19](D:/Projects/Warlock/SECURITY.md:19) omits `.wsng` from its named file formats; [SECURITY.md:28](D:/Projects/Warlock/SECURITY.md:28) claims `fetch_worker` is the single network exception despite the new pack worker. Its checkpoint-process description is also inconsistent with the subprocess architecture. | Include song import and dependency-pack installation, describe both user-initiated network exceptions, and update the process model. Keep the policy aligned with actual architecture and reporting scope. |

**Release order**

1. Close H01 and H02, including regression coverage and interrupted-pack recovery.
2. Fix persistence/export and Muse defects, especially M03, M05, and M07–M12.
3. Run H03 against the final installer, then update M16 with measured facts.
4. Resolve or explicitly disclose M14/M15 with a named hardware/interop test owner and beta instructions.
5. Re-run the default and relevant performance/GPU lanes against the final source and archive the installer hash and test results.

The previous audit in `TODO.md` was used as a lead list, not accepted verbatim. Examples of corrections here: the README opening already says thirteen; the TRELLIS weight check is now nonfatal; basic Tiled interchange has external evidence. Previously identified issues that remain were retained with current source references.
